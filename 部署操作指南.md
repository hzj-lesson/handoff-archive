# GitHub Pages 部署操作指南

> 适用：`hzj-lesson` 名下 6 个平台 + 1 个归档仓库。**从零到上线照着做即可**，不需要 git 命令行。

---

## 零、30 秒速查（老手直接看这）

```bash
# 1) 准备 token（每个新沙箱只需做一次）
echo 'ghp_你的token' > /tmp/gh_token

# 2) 部署（改哪个文件就 map 哪个）
python3 /workspace/部署工具/deploy.py \
  --repo hzj-lesson/bigdata-excel-contest \
  --map /workspace/index.html=index.html \
  --msg "fix: 描述你改了什么"

# 3) 看线上状态（不部署）
python3 /workspace/部署工具/deploy.py --repo hzj-lesson/bigdata-excel-contest \
  --map /workspace/index.html=index.html --check-only
```
脚本自带重试、自动校验，跑完会打印 `✅ 全部一致，部署完整`。

---

## 一、前置准备

### 1.1 拿 GitHub Token

1. 登录 GitHub → 右上角头像 → **Settings**
2. 左侧最下面 **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. **Generate new token (classic)**
4. 勾选 **`repo`**（完整仓库读写权限，部署必须要）
5. 生成后**立刻复制**（关掉页面就再也看不到）

### 1.2 存到沙箱

```bash
echo 'ghp_xxxxxxxxxxxxxxxxxxxx' > /tmp/gh_token
chmod 600 /tmp/gh_token
```

> ⚠️ **token 存在 `/tmp`，沙箱销毁就没了**（仓库里也搜不到，因为它是 secret）。
> 换新环境时重新写一次这个文件即可，脚本会自动读它。
> 也可以改用环境变量：`export GITHUB_TOKEN=ghp_xxx`，或用 `--token` 直接传。

### 1.3 确认 Pages 已开启

每个仓库 → **Settings** → **Pages** → Source 选 **`main` 分支 / root 目录**。
6 个仓库**都已开启**，新增仓库时才需要手动开。

---

## 二、两种部署方式怎么选

| 方式 | 适用场景 | 特点 |
|---|---|---|
| **Git Data API**（blob→tree→commit→ref） | **默认用它** | 一次可提交多个文件、不依赖旧文件 sha、不会因并发覆盖 |
| Contents API（`PUT /contents/...`） | 改单个小文件 | 必须先 GET 拿到文件当前 `sha` 再 PUT，多文件要循环，容易踩 409 冲突 |

**本项目一律用 Git Data API**，已封装进 `部署工具/deploy.py`。

---

## 三、各仓库部署命令对照

| 平台 | 仓库 | 本地文件 | 仓库内路径 |
|---|---|---|---|
| **Excel 竞赛平台** | `hzj-lesson/bigdata-excel-contest` | `/workspace/index.html` | `index.html` |
| 教师备课助手（根域名站） | `hzj-lesson/hzj-lesson.github.io` | 多文件（Vite 产物） | `assets/*.js`、`lessons/*.html`、`sw.js` |
| 考公学习平台 | `hzj-lesson/kaogong-learning-platform` | `考公学习平台.html` | `index.html` |
| 英语四级 | `hzj-lesson/cet4-platform` | `英语四六级学习平台.html` | `index.html` |
| 大数据学习平台 | `hzj-lesson/bigdata-learning` | — | — |
| **归档仓库** | `hzj-lesson/handoff-archive` | 见下 | 见下 |

### 3.1 Excel 竞赛平台（最常用）

```bash
python3 /workspace/部署工具/deploy.py \
  --repo hzj-lesson/bigdata-excel-contest \
  --map /workspace/index.html=index.html \
  --msg "fix: 某某修复"
```

### 3.2 归档（防沙箱销毁，建议每次改完顺手做）

```bash
python3 /workspace/部署工具/deploy.py \
  --repo hzj-lesson/handoff-archive \
  --map /workspace/index.html=bigdata-excel-contest/index.html \
  --map /workspace/交接报告.md=交接报告.md \
  --map /workspace/竞赛冲刺建议.md=竞赛冲刺建议.md \
  --map /workspace/大数据分析素养_16章考点摘要.md=大数据分析素养_16章考点摘要.md \
  --map /workspace/部署操作指南.md=部署操作指南.md \
  --map /workspace/部署工具/deploy.py=部署工具/deploy.py \
  --msg "chore: 归档"
```

---

## 四、部署后必做：验证完整性

### ⚠️ 最大的坑：别用错方法判断文件是否传全

| 方法 | 结论 |
|---|---|
| `base64.b64decode(content).decode().length` | ❌ **错**。这是 UTF-8 **字符数**，中文一个字算 1，而文件是**字节数**，中文占 3 字节 → 会误报"文件被截断" |
| `git/blobs/<sha>` 返回的 **`size` 字段** | ✅ **正确**。这是真实字节数 |
| `contents?ref=main` 返回的 `size` | ✅ 也正确，deploy.py 用的就是这个 |

**本项目踩过两次这个坑**，白折腾很久。deploy.py 已内置正确校验，跑完看 `✅ 全部一致` 就行。

### 4.1 页面级验证（更靠谱）

用 puppeteer 跑真实浏览器：

```bash
node /tmp/verify_book4.js    # 例子：验证教材考点渲染
```

关键的三个断言：
- 控制台 **0 报错**（`pageerror` / `console.error`）
- 渲染出的条目数 == 数据里的条目数
- 页面文本里 `undefined` / `[object Object]` / 空 `<li>` 均为 0

> 📌 **写测试脚本的坑**：页面里有 `confirm()` 的按钮（如「重置本卷」）会**把无头浏览器挂死**，
> 必须注册 `page.on('dialog', d => d.accept())`。
> 本平台的教材章节是 `<details>` 原生折叠，`innerText` 默认是空的，要先 `d.open = true` 才能读到内容。

---

## 五、常见故障排查

| 现象 | 原因 | 解决 |
|---|---|---|
| `HTTP 403: rate limit exceeded` | API 限流（每小时 5000 次，通常不会撞到；真撞了是共用 IP） | 等 15 分钟～1 小时。deploy.py 已自动退避重试 |
| `HTTP 409 Conflict` | 用 Contents API 时传的 `sha` 过期了（文件被别人改过） | 换 Git Data API（deploy.py），它不需要旧 sha |
| `HTTP 000` / `curl exit 35` | 沙箱网络对 GitHub 的 SNI/keep-alive 问题 | 请求头加 `Connection: close`（脚本已加）；或给 `/etc/hosts` 绑 `api.github.com` |
| 部署成功但网页没变 | Pages 还在构建，或浏览器缓存 | 等 1–2 分钟；强制刷新 `Ctrl+Shift+R`；仓库 Actions 页看构建状态 |
| 手机上还是旧版 | Service Worker 缓存 | 教师备课助手需同步 bump `sw.js` 里的缓存版本号 |
| 中文文件名乱码 | URL 未编码 | deploy.py 已直接传 UTF-8 路径，一般无问题 |

---

## 六、从零恢复（换新沙箱 / 交接给别人）

按顺序执行：

```bash
# 1. 装 puppeteer（用于页面验证，可选）
cd /tmp && npm i puppeteer-core

# 2. 写 token
echo 'ghp_xxx' > /tmp/gh_token

# 3. 从归档仓库把项目拉回来（不要从零重写！）
curl -L -o /workspace/index.html \
  https://raw.githubusercontent.com/hzj-lesson/handoff-archive/main/bigdata-excel-contest/index.html

# 4. 验证拉回来的文件完整
python3 /workspace/部署工具/deploy.py \
  --repo hzj-lesson/bigdata-excel-contest \
  --map /workspace/index.html=index.html --check-only
```

> ✅ **habit**：改完立刻归档到 `handoff-archive`。`/workspace` 和 `/tmp` 都会随沙箱销毁，
> 归档仓库是唯一持久化的地方。

---

## 七、改数据时的安全约定

`bigdata-excel-contest` 是**单文件前端**，数据在 `index.html` 的 `/*DATA*/{...}/*DATA*/` 标记内以 JSON 内联。

改数据时务必：
1. 用 Python 读 JSON、改完 `json.dumps(ensure_ascii=False, separators=(',',':'))` 写回
2. **写回前后做字段级 diff 断言**，确认只有目标字段变动（例：改解析就断言只有 `ex` 变）
3. 部署前先在浏览器里跑一遍

```python
import re, json
s = open('index.html', encoding='utf-8').read()
m = re.search(r'(/\*DATA\*/)(.*?)(/\*DATA\*/)', s, re.S)
d = json.loads(m.group(2))
# ... 改 d ...
diff = [k for k in old if old[k] != d[k]]
assert diff == ['ex'], '非预期字段变更: %s' % diff   # 只应改 ex
open('index.html','w',encoding='utf-8').write(
    s[:m.start(2)] + json.dumps(d, ensure_ascii=False, separators=(',',':')) + s[m.end(2):])
```
