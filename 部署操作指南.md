# GitHub 部署操作指南（交接版）

> 最后更新：**2026-09-10**
> 本文档用于**整体交接**。拿到它的人，不需要任何口头补充，就能接管 `hzj-lesson` 名下全部 8 个仓库、复现整套部署环境。
> 文档里含有真实 token，**请当作密码保管，不要贴进公开场合**。

---

## 一、账号与凭证

### 1.1 账号

| 项 | 值 |
|---|---|
| GitHub 用户名 | **`hzj-lesson`** |
| 账号类型 | 个人账号（User），非组织 |
| 注册时间 | 2026-08-29 |
| 套餐 | GitHub Free（免费版） |
| 主页 | https://github.com/hzj-lesson |

> ⚠️ **免费版的两个硬限制**（已实测踩过）：
> 1. 免费个人账号**无法给普通仓库开 Pages** —— `POST /repos/{o}/{r}/pages` 会返回
>    `422 Your current plan does not support GitHub Pages for this repository`。
>    **但 `用户名.github.io` 这个根站仓库例外，Pages 是自动开的。**
>    所以本账号的做法是：能开 Pages 的仓库各自开；开不了的就**塞进根站仓库的子目录**。
> 2. 私有仓库的 Pages 也不在免费额度内（`family-menu` 就是私有仓，Pages 未开，实测走根站子目录发布）。

### 1.2 Token（部署用）

```
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx（明文见本地完整版，或向负责人索取）
```

> 🚨 **不要把本文件 push 到公开仓库！**
> GitHub 有 secret scanning，含明文 token 的提交会被**直接 422 拒绝**
> （实测报错：`Repository rule violations found / Secret detected in content / GITHUB_PERSONAL_ACCESS_TOKEN`）。
> 推成功了更糟——等于把 token 公开。
> **正确做法**：本文件只保存在本地 / 加密渠道转发；推到 GitHub 的是打码版（token 写成 `ghp_xxxx…xxxx`）。

| 项 | 值 |
|---|---|
| 类型 | Personal access token **classic**（前缀 `ghp_`） |
| 长度 | 40 位 |
| **权限** | **`repo`**（完整仓库读写，含私有仓库） |
| 缺失权限 | 无 `read:org`（导致 `gh auth login` 会失败，见 §6.4）、无 `workflow`、无 `delete_repo` |

**存放方式**（脚本按此优先级读取：`--token` 参数 → 环境变量 `GITHUB_TOKEN` → 文件）：

```bash
echo 'ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx（明文见本地完整版，或向负责人索取）' > /tmp/gh_token
chmod 600 /tmp/gh_token
```

> 🔒 **交接后必做（安全）**
> 这个 token 写在文档里就等于已经暴露。**交接完成后请立刻吊销并生成新的**：
> GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → 找到这条 → **Delete**
> → Generate new token (classic) → 勾选 `repo` → 复制新 token → 替换本文档 §1.2 的值 → 重新归档。
> 忘记吊销最坏后果：拿到它的人可以**删掉你所有仓库**。

### 1.3 环境依赖

零第三方库，只要 Python 3 标准库 + 能访问 `api.github.com`。**不需要安装 git**，全部走 REST API。

```bash
# 可选：页面级验证用的无头浏览器
cd /tmp && npm i puppeteer-core
# 系统需有 chromium（/usr/bin/chromium）
```

---

## 二、仓库总览（8 个）

`family-menu` 是**私有仓库**，用 `GET /users/hzj-lesson/repos` 只能看到 7 个公开的；
要用 `GET /user/repos?type=all` 才能看到全部 8 个。**这是最容易漏掉的一个坑。**

| # | 仓库 | 可见性 | 体积 | 默认分支 | Pages 地址 | 最近推送 |
|---|---|---|---|---|---|---|
| 1 | `hzj-lesson.github.io` | public | 180 KB | main | **https://hzj-lesson.github.io/** | 2026-09-10 |
| 2 | `bigdata-excel-contest` | public | 6 KB | main | https://hzj-lesson.github.io/bigdata-excel-contest/ | 2026-09-09 |
| 3 | `handoff-archive` | public | 4 KB | main | —（不开 Pages，纯归档） | 2026-09-10 |
| 4 | `family-menu` | **private** | 0 KB | main | —（免费版不给私有仓开 Pages） | 2026-09-10 |
| 5 | `english-cet-learning` | public | 0 KB | main | https://hzj-lesson.github.io/english-cet-learning/ | 2026-09-07 |
| 6 | `cet4-platform` | public | 0 KB | main | https://hzj-lesson.github.io/cet4-platform/ | 2026-09-07 |
| 7 | `kaogong-learning-platform` | public | 0 KB | main | https://hzj-lesson.github.io/kaogong-learning-platform/ | 2026-09-01 |
| 8 | `bigdata-learning` | public | 1 KB | main | https://hzj-lesson.github.io/bigdata-learning/ | 2026-09-01 |

全部 8 个仓库 **Pages 状态均为 `built`**（`handoff-archive` 和 `family-menu` 未开 Pages，属预期）。

---

## 三、各仓库内容与用途

### 3.1 `hzj-lesson.github.io` —— 根站（最重要）

**它既是门户首页，也是其他站点的"托管宿主"**。1493 个文件，180 KB（主要是教材 JSON）。

```
/index.html            5935    门户首页（列出所有平台入口）
/family-menu/index.html 69448  ⭐ 点菜单 v6（真正对外访问的地址）
/ai-tools.html         30624
/lessons/                      教师备课助手生成的讲义页
/assets/                       Vite 构建产物（JS/CSS/KaTeX 字体/教材数据包）
/sw.js                 2618    Service Worker（改版要 bump 缓存版本号）
/sitemap.xml   /robots.txt   /manifest.webmanifest   /og-image.png
```

### 3.2 `bigdata-excel-contest` —— Excel 竞赛平台（主项目）

```
/index.html           623915   单文件前端，数据在 /*DATA*/{...}/*DATA*/ 内以 JSON 内联
/print.html            25281   打印版
/files/*.xlsx                  3 个练习数据集
/files/print-handbook.pdf  4.1 MB
/files/question-bank.html  46792
```
最近提交 `797d3474`（2026-09-09）：省赛模拟卷三 136 题补充真实解析。

### 3.3 `handoff-archive` —— 归档仓库（交接命脉）

**沙箱 /workspace 和 /tmp 随时会被销毁，只有这个仓库是持久的。改完东西一定要顺手归档。**

```
/交接报告.md              41810   ⭐ 全项目交接报告（A~N 节）
/部署操作指南.md           9825    本文档
/部署工具/deploy.py        6462
/部署工具/gh.py           11201
/bigdata-excel-contest/index.html
/cet4-merged-platform/index.html
/excel-print/print.html + print.pdf
/family-menu/v3~v6-*.html + README      四个历史版本快照
/dsh-plugin-textbook/                    DeepSeek 教材→讲义插件
/screenshots/*.png                       8 张截图
/大数据分析素养_16章考点摘要.md
/竞赛冲刺建议.md
```

### 3.4 `family-menu` —— 点菜单（私有）

```
/index.html   69448   v6：16 品类 / 90 品牌 / 360 款，无价格，双角色（她点单→你下单）
/README.md     4828
```
提交线：`df16232d`(v4 双角色) → `04b5dab5`(v5 全品类) → **`ddfd6604`**(v6 去价格+16品类+修复底部遮挡)。
⚠️ 私有仓开不了 Pages，线上实际访问 **https://hzj-lesson.github.io/family-menu/**（根站子目录那份同步的副本）。

### 3.5 其余四个

| 仓库 | 内容 | 说明 |
|---|---|---|
| `cet4-platform` | `index.html`(168158)、`files/cet4-vocab.html` | 四六级合并统一版（**当前主力**） |
| `english-cet-learning` | `英语四六级学习平台.html`(126502)、`index.html`(617 跳转页) | 旧版，已 redirect 到 cet4-platform |
| `kaogong-learning-platform` | `考公学习平台.html`(93075)、`docs/沙箱连通GitHub经验方法.md` | 2028 国考备考版 |
| `bigdata-learning` | `index.html`(602582)、`学习平台.html`(602582，重复) | 大数据管理与应用 |

---

## 四、部署：两步走

### 4.1 拿回脚本

```bash
# 方式 A：整个归档仓库拉回来（推荐）
python3 部署工具/gh.py clone handoff-archive /workspace/_archive

# 方式 B：只要两个脚本
python3 部署工具/gh.py pull handoff-archive 部署工具/deploy.py /workspace/部署工具/deploy.py
python3 部署工具/gh.py pull handoff-archive 部署工具/gh.py    /workspace/部署工具/gh.py
```

> 🐔🥚 问题：脚本在归档仓库里，而拉仓库又要脚本。
> 解法：`gh.py` 的逻辑极简，可以用一行 curl 先救急，或者直接用 `git clone https://github.com/hzj-lesson/handoff-archive.git`。

### 4.2 部署

```bash
export GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx（明文见本地完整版，或向负责人索取）

# 单文件
python3 /workspace/部署工具/deploy.py \
  --repo hzj-lesson/bigdata-excel-contest \
  --map /workspace/index.html=index.html \
  --msg "fix: 描述你改了什么"

# 多文件（一次提交，原子生效）
python3 /workspace/部署工具/deploy.py \
  --repo hzj-lesson/handoff-archive \
  --map /workspace/index.html=bigdata-excel-contest/index.html \
  --map /workspace/交接报告.md=交接报告.md \
  --map /workspace/部署操作指南.md=部署操作指南.md \
  --msg "chore: 归档"

# 只看线上状态，不部署
python3 /workspace/部署工具/deploy.py --repo hzj-lesson/bigdata-excel-contest \
  --map /workspace/index.html=index.html --check-only
```

跑完看最后一行：**`✅ 全部一致，部署完整`** 才算成功。

### 4.3 点菜单（family-menu）要部署两处

因为私有仓开不了 Pages，每次改完必须同步推两个地方：

```bash
# 1) 私有仓（源码归档）
python3 部署工具/deploy.py --repo hzj-lesson/family-menu \
  --map /workspace/点菜单/index.html=index.html --msg "feat: ..."

# 2) 根站子目录（真正能被访问到的那一份）
python3 部署工具/deploy.py --repo hzj-lesson/hzj-lesson.github.io \
  --map /workspace/点菜单/index.html=family-menu/index.html --msg "feat: ..."
```

---

## 五、`gh.py` 日常命令

```bash
export GH_TOKEN=$(cat /tmp/gh_token)
python3 /workspace/部署工具/gh.py status                     # 全部仓库总览
python3 /workspace/部署工具/gh.py list                       # 列出仓库+描述
python3 /workspace/部署工具/gh.py ls handoff-archive [路径]   # 列文件
python3 /workspace/部署工具/gh.py pull handoff-archive 交接报告.md    # 下载单文件（支持中文名）
python3 /workspace/部署工具/gh.py clone bigdata-excel-contest          # 整仓 tarball
python3 /workspace/部署工具/gh.py push <repo> <本地>=<路径> --msg "..." # 上传（等价 deploy.py）
python3 /workspace/部署工具/gh.py log bigdata-excel-contest 5         # 最近 5 条提交
python3 /workspace/部署工具/gh.py pages bigdata-excel-contest         # Pages 网址+构建状态
```

`repo` 可简写，`bigdata-excel-contest` 自动补成 `hzj-lesson/bigdata-excel-contest`。

---

## 六、原理与踩过的坑

### 6.1 为什么用 Git Data API

| 方式 | 结论 |
|---|---|
| **Git Data API**（blob→tree→commit→ref） | ✅ **默认用它**。可一次提交多文件、不需要旧文件 sha、并发不覆盖 |
| Contents API（`PUT /contents/...`） | ❌ 必须先 GET 拿 sha，多文件要循环，容易 409 |

`deploy.py` 的四步：**建 blob → 基于 base_tree 建新 tree → 建 commit → PATCH ref**。

### 6.2 判断文件是否传全 —— 别用错方法

| 方法 | 结论 |
|---|---|
| `len(base64.b64decode(content).decode())` | ❌ **错**。这是 UTF-8 **字符数**，中文 1 字算 1，而文件是**字节数**（中文 3 字节）→ 必然误报"被截断" |
| `git/blobs/<sha>` 的 **`size` 字段** | ✅ 正确 |
| `contents?ref=main` 的 `size` | ✅ 也正确（deploy.py 用的这个） |

**这个坑白折腾了两次。**

### 6.3 中文路径必须 URL 编码

仓库里全是中文文件名（`交接报告.md`、`英语四六级学习平台.html`），
直接拼进 URL 会崩在 ascii 编码上。脚本里已处理：

```python
urllib.parse.quote(path, safe='/')   # safe='/' 保留目录分隔符
```

### 6.4 `gh` CLI 用不了

`gh auth login` 会失败，因为 token 缺 `read:org` scope。
绕法：`export GH_TOKEN=$(cat /tmp/gh_token)` 后 `gh repo list hzj-lesson` 正常。
根治：去 token 设置页补勾 `read:org`，再 `gh auth login --with-token < /tmp/gh_token`。

### 6.5 沙箱网络

- 请求头必须带 **`Connection: close`**（否则 keep-alive 故障，报 `HTTP 000` / curl exit 35）
- 大响应会 `IncompleteRead`（已实测）：**重试 5~8 次**，脚本已内置退避
- `curl`/WebFetch 访问 `hzj-lesson.github.io` 在本沙箱会 `SSL_ERROR_SYSCALL`（代理问题），
  **验证部署请走 GitHub API，不要走 HTTP 抓页面**

### 6.6 其他

| 现象 | 原因 / 解法 |
|---|---|
| `HTTP 403 rate limit` | 每小时 5000 次，等 15~60 分钟，脚本已自动退避 |
| 部署成功网页没变 | Pages 构建要 1~2 分钟；`Ctrl+Shift+R` 强制刷新 |
| 手机还是旧版 | Service Worker 缓存，需 bump `sw.js` 里的版本号 |
| 页面 `confirm()` 挂死无头浏览器 | 必须 `page.on('dialog', d => d.accept())` |
| 无头浏览器截图 emoji 是方块 | 装 `fonts-noto-color-emoji` |

---

## 七、从零恢复（新沙箱 / 新接手人）

```bash
# 1. 装可选依赖
cd /tmp && npm i puppeteer-core

# 2. 写 token
echo 'ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx（明文见本地完整版，或向负责人索取）' > /tmp/gh_token
chmod 600 /tmp/gh_token

# 3. 从归档仓库拿回一切（不要从零重写！）
python3 部署工具/gh.py clone handoff-archive /workspace/_archive

# 4. 校验完整性
python3 部署工具/deploy.py --repo hzj-lesson/bigdata-excel-contest \
  --map /workspace/index.html=index.html --check-only
```

**习惯**：改完立刻归档到 `handoff-archive`。`/workspace` 和 `/tmp` 随沙箱销毁，归档仓库是唯一持久化处。

---

## 八、改数据时的安全约定

`bigdata-excel-contest` 是单文件前端，数据内联在 `/*DATA*/{...}/*DATA*/` 标记内。
改数据务必：**JSON 读写 + 字段级 diff 断言 + 浏览器跑一遍**。

```python
import re, json
s = open('index.html', encoding='utf-8').read()
m = re.search(r'(/\*DATA\*/)(.*?)(/\*DATA\*/)', s, re.S)
d = json.loads(m.group(2))
old = json.loads(json.dumps(d))     # 快照
# ... 改 d ...
diff = [k for k in old if old[k] != d[k]]
assert diff == ['ex'], '非预期字段变更: %s' % diff    # 只应改 ex
open('index.html','w',encoding='utf-8').write(
    s[:m.start(2)] + json.dumps(d, ensure_ascii=False, separators=(',',':')) + s[m.end(2):])
```

---

## 九、交接检查清单

拿到这个文档的人，按顺序确认：

- [ ] 能用 §1.2 的 token 调通 `GET https://api.github.com/user`，返回 `login: hzj-lesson`
- [ ] `gh.py status` 能看到 **8 个仓库**（含私有的 `family-menu`；只看到 7 个说明用了 `/users/` 接口而非 `/user/repos?type=all`）
- [ ] `gh.py clone handoff-archive` 能拉回 `交接报告.md` 和两个脚本
- [ ] 打开 https://hzj-lesson.github.io/ 门户首页正常
- [ ] 打开 https://hzj-lesson.github.io/family-menu/ 点菜单正常
- [ ] 打开 https://hzj-lesson.github.io/bigdata-excel-contest/ 竞赛平台正常
- [ ] **（交接方）已吊销旧 token 并生成新 token，替换 §1.2 后重新归档本文档**
