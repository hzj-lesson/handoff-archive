# 账号与平台交接报告

> 生成时间：2026-09-08 19:00（GMT+8）　|　上一版：2026-09-07
> 适用对象：接手 `hzj-lesson` GitHub 账号的新同事/新账号
> 目的：用一份文档说清「账号里有什么、最近改了什么、怎么部署、还有哪些坑和待办」，让交接不再靠口述。
> **本版更新重点**：Excel 竞赛平台（`bigdata-excel-contest`）交互形态重构为「逐题练习」+ 修复 10 个 bug + 删除限时模考 + 新增单题重做 + **新增「📚 教材考点」16 章**。见第三节 A、B、F。
>
> ⚠️ **改动有两个来源**：15:45 之前是 AI 会话按用户要求做的（第三节 A~E）；**18:16 / 18:34 两次提交是用户自己（另一会话）做的**（第三节 F）。接手时注意区分，不要以为是同一次改动。

---

## 一、账号与访问信息

| 项目 | 内容 |
|---|---|
| GitHub 账号 | **hzj-lesson**（全部仓库为 public，且全部开启 GitHub Pages） |
| 仓库总数 | **7 个**（另有 1 个归档仓库 `handoff-archive`） |
| 访问令牌 | 存于**当前沙箱** `/tmp/gh_token`，**不随账号转移**。新环境需用自己的 token 重新生成：`gh auth token` 或 Personal Access Token，写回同名文件即可 |
| 运行环境 | Node.js 22.19.0（经 nvm 安装，**必须 ≥22.19**，原因见第三节 E）；puppeteer-core + `/usr/bin/chromium` 用于无头冒烟测试；tesseract + chi_sim 中文 OCR 已装 |
| 域名 | 根域名站 `hzj-lesson.github.io` 即「教师备课助手」 |

⚠️ **交接重点**：沙箱（含 token、nvm、已装插件）是本机环境，不会跟着账号走。新接手人要在自己的机器/沙箱里重新：① 配 GitHub token；② `nvm install 22.19.0`；③ 重装 dsh 自定义插件（见第三节 E）。

⚠️ **沙箱销毁风险**：`/workspace` 里的文件在沙箱关闭后会丢失。**关键交付物已归档到 `handoff-archive` 仓库**（见第六节），接手人优先从该仓库取，不要依赖本地沙箱。

---

## 二、7 个平台清单（全部在线可用）

| # | 仓库名 | 在线地址 (Pages) | 最近部署 commit | 说明 |
|---|---|---|---|---|
| 1 | bigdata-learning | https://hzj-lesson.github.io/bigdata-learning/ | — | 大数据管理与应用·个人学习平台 |
| 2 | kaogong-learning-platform | https://hzj-lesson.github.io/kaogong-learning-platform/ | — | 考公学习平台（2028 国考版） |
| 3 | hzj-lesson.github.io | https://hzj-lesson.github.io/ | `046b3658` | 教师备课助手（根域名站，九年级英语 14 单元、语文 300 章等） |
| 4 | **bigdata-excel-contest** | https://hzj-lesson.github.io/bigdata-excel-contest/ | **`0d030666`** | 全国大学生大数据分析技能大赛 Excel 赛项平台（**本轮重点重构**，397068 字节） |
| 5 | cet4-platform | https://hzj-lesson.github.io/cet4-platform/ | `2295780` | 英语四级 CET-4 备考平台（与四六级平台合并后） |
| 6 | english-cet-learning | https://hzj-lesson.github.io/english-cet-learning/ | — | **已改为自动跳转到 cet4-platform**（不再独立维护） |
| 7 | **family-menu** | **https://hzj-lesson.github.io/family-menu/** | `ddfd6604` | 🍱💕 **给她点外卖 · 她点单你下单**（9-10 v6）：他把链接发她 → 她挑 → **微信推送通知他** → 他去外卖 App 下单。**16 大品类 90 品牌 360 款**，**菜单无价格**。单文件 69KB 纯前端（v1~v5 见 `handoff-archive/family-menu/`）

> 平台 1、2 未改动，仅作清单记录。仓库地址均为 `github.com/hzj-lesson/<仓库名>`。

⚠️ **`family-menu` 的 Pages 走了根站子路径**，不是独立 Pages：用 API 给它开 Pages 报
`Your current plan does not support GitHub Pages for this repository`（个人免费账户限制，网页端
Settings → Pages 手动开则可以）。所以代码放在根站仓库的 `/family-menu/index.html`，
访问 `hzj-lesson.github.io/family-menu/`；`hzj-lesson/family-menu` 仓库只作源码托管（含 README）。
**若日后在网页端给该仓库开了独立 Pages，它会接管同一个 URL**，届时记得把根站里的子目录删掉避免混淆。

### `bigdata-excel-contest` 版本演进（接手人看这里就知道改到哪了）

| commit | 日期 | 内容 |
|---|---|---|
| `7d11c369` | 9-07 | 重构为单线学习路线，632 题去重 |
| `2c4ced9d` | 9-08 | 精简：剥离零散题与专项，**只保留两套官方模拟题**（285 题） |
| `03a47e68` | 9-08 | 修复判断题判分等 bug，加计时模考 |
| `49d289c` | 9-08 | **交互形态改为逐题练习** + 做题记录面板 |
| `4a8509f` | 9-08 | 修复 7 处一致性 bug |
| `245ce2b` | 9-08 15:45 | **彻底删除限时模考** + 新增单题重做 |
| `b9a6b473` | 9-08 18:16 | **（用户自己做的）** 新增「📚 教材考点」16 章（教材 OCR） |
| `38c4be25` | 9-08 18:34 | **（用户自己做的）** 补全缺失 CSS 变量 |
| `b52a2d3` | 9-08 19:25 | **修复手机端布局 bug**：题目区被答题卡压扁（211248 字节） |
| `7912aa7` | 9-08 20:20 | **修正 12 道题「解析与标准答案矛盾」**（211787 字节） |
| `d89d844` | 9-08 20:35 | **补全 39 道多选题逐项解析** + 修卷二第121题选项笔误（216648 字节） |
| `4db226a1` | 9-08 22:56 | **（用户自己做的）** 灌入 304 页教材 OCR 全文（1145 条），随后「每条压成一句话考点」截断到 856 条（**这是后来知识点残缺的根因**，424276 字节） |
| **`0d030666`** | 9-09 08:35 | **教材考点大清理**：考点 852→599 条，删残句、修 OCR 错字、去断词空格、拆粘连题（**当前线上版**，397068 字节） |

---

## 三、变更记录（按时间倒序）

### A.（9-08 本轮重点）bigdata-excel-contest 交互形态重构

**用户诉求**：「我要变成那种做题形式而不是考试形式，做一道出一道答案，然后做错了进入错题本，要保留做题记录。」

**改动**：

1. **逐题练习模式**（替代原计时模考成为主入口）
   - 每套卷主按钮「📝 开始练习」→ 全屏逐题：点选项**立即出答案**（✅/❌ + 高亮正确项 + 解析），多选题点「提交答案」后判分。
   - 右侧实时看板：本卷已练数、当前正确率、题号彩色导航（绿=对 / 红=错 / 灰=未练），可跳题。
   - **接着上次进度**：重新进入自动跳到第一道未练的题，并提示「已跳到第 N 题接着练」。

2. **做题记录面板**（新增「📊 做题记录」区块）
   - 总练习题数、总正确率（≥80% 绿、否则橙）；各卷进度 + 最近练习时间；**最近 30 条练习流水**（卷别+第几题+✅/❌+时间）。
   - 全程持久化，刷新/关页不丢。

3. **单题重做**：练习模式有「🔄 重做本题」，卷子展开区每题有「🔄 重做」。重做后按新结果重新计入记录（重做答对，记录从"错"改回"对"）。

4. **彻底删除限时模考**：删掉「⏱ 限时模考」按钮、123 行考试 JS（startExam/交卷评分/倒计时/答题卡）、考试专属 CSS（成绩报告/计时器/脉冲动画）。文件从 204005 → **196415 字节**。
   > 注意：`.exam` 那套**布局样式保留**（逐题练习的全屏界面复用了它，删了界面就散了）。

5. **统一答题入口**：新增 `commitAnswer()`，让「逐题练习 / 展开直接点 / 错题重练」三条路径行为完全一致（此前各写各的，导致行为不一致，见 Bug #9）。

### B.（9-08）Bug 修复清单 —— 共 10 个

**数据层审计结论：题库 285 题零异常**（无重复 id、无重复题干、答案无越界、无缺解析、两卷无 id 冲突）。问题全在代码逻辑层。

| # | 严重度 | 问题 | 影响 |
|---|---|---|---|
| 1 | 🔴 致命 | **判断题判分逻辑写反** | 80 道判断题（占 285 题的 28%）答案存字母 `A/B`，但旧代码拿选项文字「正确/错误」和字母比，永远不相等 → **判断题永远判错、正确答案永远不亮、错题本全是假错题**。改为统一按字母判分 |
| 2 | 🟠 | 模考计时显示错误 | `("0"+120).slice(-2)` 得 `"20"` → 显示成 `20:00`（已随模考删除） |
| 3 | 🟡 | 各卷「最近练习」永远显示 `—` | `recAnswer` 漏写 `r.last` 时间戳，而面板读的正是它 |
| 4 | 🔴 | **换设备同步会丢做题记录** | 同步码只打包「学习进度+错题本」，没打包做题记录和已练进度 → 换设备后记录归零（与"保留做题记录"直接冲突）。已升级同步码到 v3 |
| 5 | 🟠 | 重新进入练习不接着上次 | 每次从第 1 题开始，而答过的题锁定，得一路点几十次「下一题」 |
| 6 | 🟡 | 重置本卷后顶栏「已练」不回退 | 只清了记录没清 `answered` |
| 7 | 🟠 | 错题重练答对了，记录仍记为错 | 重练只移出错题本，不回写记录 → 正确率不反映真实水平 |
| 8 | 🟡 | 限时模考交卷不计入记录 | 顶栏「已练」涨了，记录纹丝不动（已随模考删除） |
| 9 | 🔴 | **展开题直接点，答错不进错题本** | 只有走「开始练习」才进错题本；在卷子下方展开题目直接点，只算"已练"，**答错既不进错题本也不进记录** |
| 10 | 🟡 | 当场答完的题不显示「重做」按钮 | 按钮是渲染时按记录生成的，当场作答只加了 `done` 类没重渲染 → 只有刷新后才出现。已改为答完就地重渲染 |

> Bug #1 是本轮最值钱的发现：**如果你之前用旧版练过题，当时的分数不能信**（近三成题目永远判错）。

### F.（9-08 18:16 / 18:34）「📚 教材考点」区块 —— **用户自己做的，非本 AI 会话**

> 这两次提交发生在本 AI 会话最后一次部署（15:45 `245ce2b`）**之后**，是用户自己（另一会话）完成的。已核验：**没有覆盖掉前面的任何改动**（逐题练习、做题记录、单题重做、判断题修复、删模考全部仍在，实测 0 报错）。

**改了什么**：

| commit | 时间 | 内容 |
|---|---|---|
| `b9a6b473` | 18:16 | 新增「📚 教材考点」区块：官方指定教材《大数据分析素养》（北京大数据协会编 · 304 页扫描本）**OCR 提取**，共 **16 章** |
| `38c4be25` | 18:34 | 补全 `:root` 缺失的 CSS 变量 `--primary-foreground` / `--muted-foreground` |

**实现细节**：
- 数据在 `DATA` 里**新增 `book` 字段**（16 章），每章结构：`n`(章号) `title` `part`(分部) `goals`(学习目标) `mind`(思维导图关键词数组) `sections`(章节结构数组)。
- 渲染函数 `bookHTML(c)`，输出可折叠 `<details class="bkch">`；渲染语句带保护：`if(DATA.book&&document.getElementById("book"))`。
- 区块位置：学习路线（`#route`）与两套模拟题（`#prac`）之间。
- 分部：第一部分「大数据分析基础」/ 第二部分「相关法律、伦理与职业道德」。
- 新增 CSS 类：`.bkch` `.bkno` `.bkti` `.bkpart` `.bkbody` `.bkmind` `.bksec`（均已定义）。
- 整个文件被重新排版（格式化），所以与 15:45 版对比 diff 显示全量差异，属正常。

**健康检查（已实测）**：16 章全部渲染 ✓　字段无缺失（学习目标/思维导图/章节结构 0 缺失）✓　章号 1–16 连续 ✓　CSS 变量生效（`--primary-foreground: #ffffff`、`--muted-foreground: #64748b`）✓　原有功能未受影响 ✓　控制台 0 报错 ✓

**体积变化**：196415 → **209708 字节**（+13293）。

### G.（9-08 19:25）修复手机端布局 bug（commit `b52a2d3`）

**问题**：手机（<680px）上进入逐题练习，**题目区被压成一条缝，答题卡 146 个题号占满整屏**。

**根因**：手机端 `.exam-body` 变纵向排列后，`.exam-main{flex:1}`（basis=0 可缩）被内容极高的 `.exam-side{flex:0 0 auto}`（不缩）压扁——146 题号按 8 列排约 19 行，把题目挤到约 60px。

**修复**：
1. `.exam-main` 手机端改 `flex:0 0 auto`（按题目内容自然撑开，优先显示题目）。
2. **题号导航改可折叠 `<details class="navfold">`**：手机默认收起（只显示「📋 题号导航（点开跳题）」一条），桌面默认展开；用 `pracGridOpen` 记住用户手动切换的状态（重渲染不丢）。
3. 手机端题号网格 8→**10 列**、格子改扁矩形（`aspect-ratio:auto`），题干/选项字号微调大（15.5/14.5px）。
4. 跳题后**自动收起导航**并滚回题目顶部；顶栏窄屏只显示「正确率 X%」防换行。

**实测**（390×844 手机视口 + 1280×800 桌面）：题目区从约 60px → **255px 完整可见**；答题卡折叠后侧栏仅 215px；跳题/判分/重做正常；桌面布局不变；0 报错。

### H.（9-08 20:20）修正 12 道题「解析与标准答案矛盾」（commit `7912aa7`）

**起因**：用户刷题时发现**第 47 题解析和答案自相矛盾**——题目问「日期型数据叙述**错误的**是」，答案给 C「日期字符串必须加引号」，但解析里那句「在公式中引用日期型常量时必须给日期字符串加引号」恰恰把 C 论证成了**正确**的说法。

**全库排查方法**（日后可复用，见第五节待办）：
1. 正则扫所有解析里的「选 X」字母 vs `q.a`——**只扫单选/判断**（多选题答案本就是 `"AC"` 这类组合，会 39 条误报）。
2. 反向扫：题干是**否定型**（含「错误的是/不正确的是/不属于/不是」）但解析里一个否定词都没有 → 疑似论证方向写反。全库 70 道否定型题，命中 14 道，人工复核后确认 3 道真错。
3. 反向扫：题干是**肯定型**但解析写了「不正确项/错误项」→ 命中 3 道，确认 1 道真错。

**结论：12 道题的标准答案（官方卷给的答案）全部正确，错的都是解析字段 `ex`。** 分两类：

| 类型 | 题号（省赛模拟卷一） | 说明 |
|---|---|---|
| **解析字母写错**（解析说「选 X」，答案其实是另一个字母） | 61、63、71、77、88、120 | 典型如第 63 题答案 A，解析却写「选 B」，但解析正文讲的正是「对角线是方差、非对角是协方差」→ 反而论证了 A 错 |
| **解析论证方向反了 / 完全跑题** | 47、114、80、98 | 第 114 题同 47 题毛病（问「不正确的是」，解析却把 A 论证成对的）；第 80 题问大样本判定，解析跑去讲分层比例分配；第 98 题问置信区间长度影响因素，解析跑去讲 t 检验 |
| **表述含糊顺便优化** | 49、86 | 答案对，但没说清「为什么」 |

**已全部重写解析**，均为「选 X + 为什么 X 对/错 + 其它选项各是什么 + 出处：《大数据分析素养》第 N 章」的统一格式。

> ⚠️ **第 88 题（F 分布）存在题库本身的质量问题，已知但未改**：答案 A「F 分布是一分布族」正确，但选项 D「F 分布的取值都大于零」**同样成立**（F = (U/n₁)/(V/n₂) 恒正）。这是单选题有两个正确项，官方标准答案取 A。解析里已明确标注此争议，**考场上按 A 答**。

**修改方式**：Python 读入 `/*DATA*/` 内联 JSON → 只改 `ex` 字段 → `json.dumps(ensure_ascii=False, separators=(',',':'))` 写回。写回前后逐题逐字段 diff 校验，**确认只有 12 个 `ex` 字段变动**，题干/选项/答案/ id 一个没动。

**实测**：246 道单选/判断题中「解析字母 ≠ 答案」从 6 → **0**；页面渲染正常，第 47 题点 C 后解析正确显示；控制台 0 报错。线上 = 本地 = **211787 字节**。

### I.（9-08 20:35）补全 39 道多选题逐项解析（commit `d89d844`）

**起因**：上一轮发现多选题解析只提了首个正确选项，学生做完只知道"选什么"、不知道"其它选项为什么错"。

**动手后发现比预想严重——不只是简略，还有硬伤**：

| 题号 | 问题 |
|---|---|
| 卷一 第121题 | 🔴 **解析直接写「C 不对」，而 C 就是答案之一**（答案 AC）。原解析把 D 的内容（从垃圾数据中发现价值）当成 C 来讲，且否定了正确答案 |
| 卷一 第122题 | 解析说风险保留"理性或非理性"都有，但答案 BDE 不含 C（非理性），自相矛盾 |
| 卷二 第127题 | 问「统计推断的内容**不包括**」，答案 ABCE，但 A「点估计和区间估计」严格说**属于**统计推断——题库本身有争议，已在解析中标注「以官方标准答案为准」 |

**统一新格式**：`选 XX。` + **每个选项逐个标注**（A✓/B✗…）+ 理由 + `出处：《大数据分析素养》第 N 章`。否定型题干（问"错误的是/不包括"）额外在开头注明**「本题选错误说法，入选的是表述错误的选项」**，避免又出现第 47 题那种方向搞反的阅读歧义。

**顺手修的数据瑕疵**：卷二第 121 题 E 选项原文是 `E如果总体服从正态分布`，多了一个字母 E，渲染出来变成「E. E如果…」，已改为 `如果总体服从正态分布`。

**自动化校验脚本**（可复用，逻辑写在下面的小贴士里）：
- 每道多选题的 A–E 是否**全部**被 ✓/✗ 标注到 → 0 遗漏
- 解析首句「选 XX」的字母集合 == 答案集合 → 0 不符
- 判定符号与答案是否冲突（**否定型题要反过来判**：答案项应标 ✗、非答案项应标 ✓）→ 0 冲突

**实测**：卷一第 121 题勾选 A/C 提交后判对、解析完整显示；卷二第 121 题选项笔误已修；控制台 0 报错。线上 = 本地 = **216648 字节**。

> 📌 **写校验脚本的坑**：多选题 39 道里有 5 道是**否定型**题干。如果直接用"答案项必须标 ✓"去校验，这 5 道会全被误报成冲突。必须先判断题干是否含「错误的是/不正确的是/不属于/不包括」，再把期望符号翻转。

### J.（9-09 08:35）「📚 教材考点」残缺大清理（commit `0d030666`）

**起因**：用户截图指出教材考点板块里知识点残缺（如「了新动能，正在深刻改变人们的生产生活方式」这种断头句），要求「能补齐就补齐，不行就删掉」。

**根因定位**：不是 OCR 本身的问题，而是 `4db226a1` 这个 commit（用户自己做的）为了压缩体积，写了「每条压成一句话考点（首句 + 去噪 + **截断**）」，把 1145 条 OCR 原文硬砍成 856 条——**砍的时候只保留首句，尾巴直接丢**，于是产生大量「…」断尾句和无头残片。OCR 原始全文**不在仓库里**（仓库只有 `index.html`/`print.html`/`files/*`），磁盘上也没有，**被砍掉的尾部无法还原**。

**处理原则**（用户授权「不行的话删除一些残缺内容」）：
- 语义完整、能独立成句的 → **保留**，并把省略号尾巴裁到最后一个句号
- 能根据上下文确定原意的 → **复原**（90 余条）
- 无头残片 / 数字乱码 / 页码噪声 → **删除**

**最终结果：考点 852 → 599 条，练习 392 → 394 条**

| 处理项 | 数量 | 说明 |
|---|---|---|
| 删除无法还原的残句 | 253 | 省略号断尾、无头片段、随机数表/表格数字碎片（`14.3 2,4,6 14,12，17` 这类） |
| 复原 / 改写 | 90+ | 如第15章「划》不仅要重视…」→「**《新一代人工智能发展规划》**不仅要重视…」；「了新动能」→「**大数据技术为经济社会发展注入了**新动能」 |
| 修 OCR 错字 | — | 自已→自己、重天→重大、匿恨性→**匿名性**、天数据→**大数据**、停正→停止、自前→目前、交易合者→**交易组织者**、「大数据技术**用**」→「大数据技术**滥用**」 |
| 清除页眉页码噪声 | — | `bd`、`—167 b`、`62—`、`37-`、思维导图里的 `bda`/`大数据分析素养`/`第五章` |
| 清除 URL 片段 | 2 | `1641080795548173.htm`、`1126408145.htm？baike，2022年7月25日` |
| **消除中文断词空格** | **395** | PDF 换行导致「客观事 物的逻辑归纳」这种断词，**最影响观感**的一项 |
| 练习：拆分两题粘连 | 3 | 「…答案：C 13下述关于…」被并成一条，已拆成两题并补回题号点号 |
| 练习：修 `÷` 被识别成「一」 | 6 | 「15一100×100%」→「15÷100×100%」（注意「40一20一10法则」要除外，那是连接号不是除号） |
| 练习：删孤儿题干 | 1 | 第8章「16.在方差分析中…正确的是（」只有题干无选项无答案 |

**保留的 3 处省略号**是数学公式（`自变量（x1，x2，…，xk）`），不是残缺，别再当成 bug 删。

**可复用的清理脚本要点**：
```python
# 中文断词空格清理：只有「两侧都是 ASCII 字母/数字」才保留空格（英文单词内部）
def despace(t):
    out=[]; i=0
    while i < len(t):
        if t[i] in ' \u3000':
            j=i
            while j<len(t) and t[j] in ' \u3000': j+=1
            L=t[i-1] if i>0 else ' '; R=t[j] if j<len(t) else ' '
            if re.match(r'[A-Za-z0-9]',L) and re.match(r'[A-Za-z0-9]',R): out.append(' ')
            i=j
        else: out.append(t[i]); i+=1
    return ''.join(out)
HANG=re.compile(r'(第[一二三四五六七八九十][，、]$|一是$|二是$|三是$|包括$|如下$|以下$|[,，、;；（(]$)')  # 判断句子是否挂在半空
```

> 📌 **两个必须记住的坑**：
> 1. **别用「字符串是否相等」来判断有没有被删**——`despace` 改过文本后，几百条内容其实还在，但字符串对不上会被误判成"已删除"。要比就先对两侧都做同样的归一化。
> 2. **Python `re.sub` 的替换模板不支持 `\u0001`**（报 `bad escape \u`），要插分隔符得用 `lambda` 或 `\x01`。
> 3. **后顾断言 `(?<=...)` 必须定长**，所以「答案：C 13」这种变长前缀得改成捕获组 + 分隔符再 split。

**实测**：puppeteer 无头跑，16 张章节卡片全渲染，考点 599 条 / 练习 394 条全部有内容，`undefined` 0、`[object Object]` 0、空 `<li>` 0、控制台 0 报错。线上 = 本地 = **397068 字节**。

> ⚠️ **遗留**：第 88 题（F 分布）仍是题库双正确答案争议，已按官方答案取 A 并在解析里注明；「思考与练习」里的题目 OCR 质量参差，本轮只做了断词空格、除号、粘连三项清理，若还要精修需对照原书。

### K.（9-10）family-menu v3 重写 —— 「给她点奶茶 · 专属外卖单」（commit `ead06d1d`）

- **背景**：用户透露这个工具实际用途是**远程给女朋友点外卖**（她在西华师范大学华凤校区二期），原家庭点菜单方向不对，按需求重写为奶茶外卖单。
- **功能**：
  - `PRESET` 内置 **15 个主流品牌 60+ 款招牌饮品**（蜜雪冰城/霸王茶姬/茶百道/古茗/沪上阿姨/CoCo/喜茶/奈雪/书亦/一点点/益禾堂/甜啦啦/7分甜/瑞幸/星巴克），价格为全国参考价，页面内可改；
  - 规格选择器 `SPEC`：甜度/温度/杯型(+3)/加料(+2~5)，加价自动合计，另有「想对她说」备注；
  - 购物车按「同品+同规格」合并（`specKey()`），订单生成**排版好的纯文本**（地址/电话/每杯规格/备注/合计），一键复制后粘到美团备注或发给代拿人；
  - 默认收货地址「西华师范大学华凤校区二期」；历史订单支持复制/再来一单/标记送达；
  - 管理页：品牌/饮品增删改、**品牌开关**（美团搜不到的直接隐藏）、首页文案、JSON 备份导入导出；
  - 首页内置华凤校区外卖小提示（李园餐厅二楼奶茶窗口、20~30 元起送等调研结论）。
- **数据**：`localStorage` 键 `naicha_order_v3`；粉色主题保留（飘爱心/爱心雨/心跳 logo）。
- **测试**：puppeteer 全流程 0 报错（规格加价 6→11 验证、下单文本、再来一单、品牌开关、刷新持久化）。
- **部署**：`family-menu` 源码仓 `ead06d1d`、根站 `/family-menu/index.html` `4bd78ca0`、归档 `handoff-archive/family-menu/v3-给她点奶茶-index.html`，四处 blob sha `0b0d6881` 全部一致。
- **重要限制（已告知用户）**：无法查美团/饿了么实时配送数据，**哪些品牌真能送到华凤校区二期要用户自己在 App 里确认**，然后在管理页关掉送不到的。
- v1/v2（家庭点菜单、粉色御膳房版）备份在 `handoff-archive/family-menu/`（`index.html` 33466B 为 v2）。

### L.（9-10）family-menu v4 —— 「她点单你下单」双角色改造（commit `df16232d`）

- **需求修正**：用户说明真实流程是**他把链接发给女友 → 女友在里面点 → 通知他 → 他去外卖 App 下单**，v3（他自己挑、自己复制）方向反了，故再次改造。
- **核心：通知通道选型**（接手人若想换通道看这里）
  - 实测 5 个服务的 CORS：`jsonstorage.net` / `sctapi.ftqq.com` / `pushplus.plus` / `api.day.app` / `wxpusher.zjiecode.com` **全部允许浏览器跨域**。
  - 但**零注册共享存储这条路走不通**：`api.jsonstorage.net` 所有路径返回 `{"error":"Route not found"}`（接口已变），`jsonblob.com` 已挂（返回 Cloudflare 错误页）。所以**没有做云端共享收单箱**，改为「推送 + 复制」双保险。
  - 最终实现 `sendPush()` 支持 4 种：Server酱 Turbo（form POST `sctapi.ftqq.com/<key>.send`）、PushPlus（JSON POST）、WxPusher（JSON POST，需 appToken+UID）、Bark（GET `api.day.app/<key>/<title>/<body>`）。
- **双角色机制**：`D.role` ∈ `me`/`her`；`genLink()` 把推送配置和地址 base64 进 URL hash（`#c=`），她打开后 `readCfg()` 自动置为 `her` 角色并载入配置；她的端隐藏「通知设置 / 生成链接 / 收单箱」。
- **她的提交流程**：`submitOrder()` → 若 `pushReady()` 则 `sendPush()`，成功标 `已通知TA`，失败提示改用复制；结果由 `showDone(o, pushed)` 弹层呈现（`true`/`false`/`null` 三态）。
- **他的收单箱**：`D.inbox` 存粘贴进来的订单原文，支持复制 / 标记已下单 / 删除，作为推送的兜底。
- **测试**：puppeteer 双页面（模拟两台手机）跑通 —— 他生成链接 → 她打开跳转角色 → 选规格加价 → 提交 → 拦截 fetch 确认推送 URL 与内容正确（`sctapi.ftqq.com/SCT_TEST_KEY_123.send`，body 含「珍珠奶茶」）→ 他粘贴收单箱。**0 报错**（唯一 404 是 favicon）。
- **部署**：`family-menu` `df16232d`、根站 `/family-menu/index.html` `b0d3c23f`、归档 `handoff-archive/family-menu/v4-*`，blob 均 55002B 校验一致。
- **待用户完成**：任选一个推送服务拿 key（Server酱最省事），在「设置」里填并点「发一条测试」；然后把生成的链接微信发她。**不配也能用**（她复制订单文字发微信）。

### M.（9-10）family-menu v5 —— 从「只有奶茶」扩到全品类外卖

- **需求**：用户要求「炸鸡汉堡外卖上面的都加进去」，不再只点奶茶。
- **数据**：`PRESET` 结构升为 `[id, 名称, emoji, 品类, [[品名,价格,备注],…]]`，新增 `CATS`（8 个品类）与 `SPECS`（每品类一套规格）。共 **55 品牌 / 240 款**：
  奶茶 15 · 炸鸡汉堡 10（肯德基/麦当劳/汉堡王/德克士/华莱士/塔斯汀/Popeyes/正新鸡排/Shake Shack/派乐汉堡）· 小吃炸物 6 · 米饭快餐 6 · 粉面汤品 7 · 甜品烘焙 5 · 烧烤夜宵 3 · 便利店 3。
- **关键设计：规格按品类切换**（不是全局一套）。`catOf(b)` / `specOf(cat)` 两个助手函数；`SPECS.tea`=甜度/温度/杯型/加料，`SPECS.burger`=规格(单点/套餐+12/双人餐+25)/口味/加配(加薯条+8…)/不要，`SPECS.rice`=份量/口味/加饭，等等。购物车项新增 `cat` 字段，`renderCart` / `orderText` / `specKey` / `specExtra` 全部带品类参数。
- **UI**：点单页改**两级 tab**（品类 `#catTabs` → 品牌 `#brandTabs`），管理页新增品牌时可指定品类。
- **旧数据迁移**（`boot()` 里）：旧版品牌无 `c` 字段 → 自动补默认品类并**拼上全量新品类**，同时保留用户自定义品牌、推送配置、地址。已测：55+1 自定义 = 56 品牌，配置全部保留。
- **命名**：因不再是纯奶茶，`<title>` / 顶栏 / 订单标题 / 备份文件名统一改为「给她点外卖」。
- **测试**：`specHasSweet=false`（汉堡不再问甜度）、套餐加价 22→34→42 正确、跨品类订单文本含 🍔🧋🍚 三类图标、8 个品类 tab 全部渲染。**0 报错**。
- **改动方式**：数据量大（+7.8KB），用 Python 脚本生成数据块再 splice，逻辑改动用 34 条精确字符串替换，比整体重写省事且不易丢代码。**教训**：批量替换脚本要给每处 `replace` 传「标签」参数并断言唯一匹配，否则静默漏改。

### N.（9-10）family-menu v6 —— 去价格 + 品类翻倍 + 修底部遮挡

- **需求**：「去除价格，多一些品类」。
- **去价格（全链路）**：
  - 数据结构：`PRESET` 单品由 `[名, 价, 备注]` 改为 `[名, 备注]`；`SPECS` 各品类选项由 `[名, 加价]` 改为**纯字符串数组**。
  - 删除 `specExtra()`；购物车项不再有 `unit`，订单不再有 `total`。
  - UI：菜品卡片、规格弹层、购物车、底部结算条、订单文本、推送标题 —— **全部不再出现 ¥**（测试用正则扫 `document.body.innerText` 与订单文本断言，均为 false）。
  - 底部结算条从「¥合计」改为「N 件 / M 种」。
- **品类 8 → 16**：新增 火锅冒菜、披萨意面、日韩料理、轻食沙拉、早餐面点、水果生鲜、粥汤煲仔、冰品雪糕。品牌 55 → 90，单品 240 → 360。每个新品类都配了贴合的规格组（火锅问锅底/份量/加菜，披萨问尺寸，沙拉问酱汁…）。
- **⚠️ 修 bug（用户反馈）**：底部结算条 `.bar` 固定在 tabbar 上方，但 `body` 的 `padding-bottom` 只有 `66px`，放不下「结算条 58px + 导航 52px」，**最后一个菜品被挡住**。改为 `124px`；并加 `body.no-bar` 类，非点单页（无结算条）收窄到 `72px`，避免底部一大片空白。已用几何断言验证：最后菜品 bottom 703 < 结算条 top 726，结算条 bottom 792 = tabbar top 792（贴合不重叠）。
- **版本迁移机制**：新增 `VER=6` + `migrate()`。旧数据（有价格、只有 8 品类）加载时自动合并新品牌，**保留用户关掉的品牌开关、自定义品牌、推送配置、地址**，并 `delete i.price` 清掉旧价格字段。测试：91 品牌（90+1 自定义）、mxbc 开关保持 false、推送与地址不变、无 price 残留。
- **部署**：`family-menu` `ddfd6604`、根站 `/family-menu/index.html`、归档 `handoff-archive/family-menu/v6-*`，blob 均 69448B 校验一致。
- **教训**：改数据结构一定要配**版本号迁移**，只靠「字段缺失检测」（上一版用 `!brand.c`）在字段已存在时不会触发，用户会卡在旧数据上看不到新内容。

### C.（9-07）bigdata-excel-contest 单线学习路线重构（commit `7d11c369`）


- 去掉多 Tab 跳转，改为**一条向下滚动的学习路线**，知识点与配套题同屏。
- 12 个单元按 42 天备考计划串联，每单元带 `📅 第 X 天 · 9月Y日` 徽章。
- 题目全局去重：原 643 题 → 删 11 道重复 → 632 道。
- 另交付可打印版 `print.html` + `print.pdf`（A4，9 页，66 条必背要点）。
- **遗留**：仓库内旧 `files/` 资源已不被新页面引用，建议清理；单元 12「数据建模」无专属题，练习并到数据预处理单元。

### D.（9-07）cet4-platform + english-cet-learning 合并（commit `2295780`）

- 主站用 `cet4-platform`，`english-cet-learning` 改为 `<meta http-equiv="refresh">` 自动跳转。
- 合并后 9 模块：总览 / 逐日计划 / 知识点 / 在线刷题 / 备考方法 / 资源中心 / 单词本（669 词）/ 笔记日志 / 番茄钟，另含闪卡复习、打卡热力图。
- 资源中心按 URL 去重，新增 12 条实用资源。

### E. DeepSeek Harness 自定义插件「教材→讲义」

在官方开源底座 [deepseek-ai/deepseek-harness](https://github.com/deepseek-ai/deepseek-harness) 上写了 cordis 插件 `dsh-plugin-textbook`，跑通「教材大纲 → 结构化讲义 → 注入平台数据层」。

验证日志（沙箱真实跑通）：
```
[dsh-plugin-textbook] CONSTRUCTED (service name=textbook)
[dsh-plugin-textbook] DEMO 完成 -> {path:/workspace/demo_data-junior.json, total:1, insertedAt:0}
```

**安装约定（接手必读）**：
1. Node 必须 **≥ 22.19**（沙箱原 22.13 缺 `node:zlib.createZstdDecompress`，启动即失败）：`nvm install 22.19.0 && nvm use 22.19.0`。
2. 插件须放在 `~/.dsh/profiles/web/node_modules/@deepseek-ai/dsh-plugin-textbook`（丢进 npx 缓存会被 loader 找不到）。
3. 在 `~/.dsh/profiles/web/cordis.patch.yml` 的 `dispose` 列表加 `- name: '@deepseek-ai/dsh-plugin-textbook'`。
4. 入口是 `export default class XService extends Service`；**不能写 TS 的 `declare module`**（纯 JS 运行时不认）。
5. 启动：`DSH_DEMO=1 npx @deepseek-ai/dsh web`（浏览器开 3080）。
6. `lib/index.js` 里 `generateWithLLM` 为占位，**尚未接真实 LLM Key**。

---

## 四、关键技术约定（接手必读）

1. **单文件前端 + 数据注入**：`bigdata-excel-contest` 是单 `index.html`，数据在 `/*DATA*/{...}/*DATA*/` 标记内以 JSON 内联。改题 = 改这段 JSON。
   - **当前顶层结构（4 个 key）**：`units`（12 个学习单元，现已无内嵌题）+ `practices`（两套卷：`pr15` 省赛模拟卷一 146 题 / `pr16` 省赛模拟卷二 139 题，共 **285 题**）+ `tips` + **`book`（16 章教材考点，9-08 新增）**。
   - 题型分布：判断 80 / 单选 166 / 多选 39。
   - ⚠️ 旧文档写的 `QZBANK` / `KB1` / `KB2` 是**精简前的旧结构，已不存在**，勿按此找。
   - 单题字段：`q`(题干) `o`(选项数组) `a`(答案字母，多选如 `"ABD"`) `type`(`single`/`multi`/`judge`) `ex`(解析) `id`。
   - **答案一律以字母存储**（A/B/C/D）。判断题选项「正确/错误」对应 A/B —— 这正是 Bug #1 的根源，改判分逻辑时务必注意。

2. **localStorage 键位**（`bigdata-excel-contest` 全部状态，清缓存即丢）：
   | 键 | 用途 |
   |---|---|
   | `excel_route_v2` | 单元完成状态 |
   | `excel_route_wrong_v2` | 错题本 |
   | `excel_route_doneq_v2` | 已练题集合（顶栏「已练 X/285」） |
   | `excel_practice_records_v2` | **做题记录**（各卷 answers + 最近 60 条流水 + last 时间戳） |

3. **其他平台数据层**：教师备课助手 `data-junior` / `data-meta` / `wb-lib`；四级/四六级 `ALLQ` / `VOCAB` / `STAGES` / `RES` / `state`。

4. **部署方式**（**完整教程见 `/workspace/部署操作指南.md`，可直接执行的脚本见 `/workspace/部署工具/deploy.py`**）：
   - 单文件更新 → **GitHub Contents API**（`PUT /repos/{owner}/{repo}/contents/{path}`，需 base64 + 文件 sha）。
   - 多文件批量 / 精确控制 → **Git Data API**（blob→tree→commit→ref）。**本项目默认用它**，已封装进 deploy.py。
   - 沙箱网络对 GitHub 需 `/etc/hosts` 绑定 + `Connection: close` 头绕过 SNI/keep-alive 故障。
   - ⚠️ **校验部署是否完整**：用 `git/blobs/<sha>` 的 `size` 字段对比本地字节数。**不要**用 base64 解码后的 `.length` 判断——那是 UTF-8 字符数，中文占多字节，会误报"文件被截断"（本轮踩过两次这个坑）。
   - ⚠️ **中文文件名的路径必须 URL 编码**（`urllib.parse.quote(path, safe='/'`），否则请求直接崩在 `ascii codec can't encode`。deploy.py 已修。

5. **测试方法**：puppeteer-core + `/usr/bin/chromium` 无头跑真实交互。**注意页面里有 `confirm()` 的按钮（如「重置本卷」）必须注册 `page.on('dialog')` 自动接受，否则测试会挂死**。

6. **命名与主权**：中国台湾/中国香港/中国澳门，勿作独立国家表述。

---

## 五、待办 / 未完事项

| 项 | 说明 | 优先级 |
|---|---|---|
| 百度网盘图片 PDF 提取 | 用户想提取一份图片型 PDF 知识点做打印版，**因文件 >20MB 且为扫描图，用户未传文件，未完成**。路径：本地用 Umi-OCR 识别成 txt 发来，或压到 <20MB 上传后 tesseract 识别 | 取决于用户 |
| DeepSeek Harness 接 LLM | `generateWithLLM` 占位未接真实 Key，接上即变 AI 自动备课 | 中（需 Key） |
| 北师大数学九上 6 章 | 目前仅目录，未补完整教案。仓库历史有 `a228b840` | 中 |
| Excel 竞赛「数据建模」单元 | 无专属题，临时并到数据预处理单元 | 低 |
| bigdata-excel-contest 旧 `files/` | 已不被新页面引用，建议清理瘦身 | 低 |
| 各平台打印版 | Excel 竞赛已出 print 版；其他平台可按需生成 | 按需 |
| ~~多选题解析太简略~~ | ✅ **已完成**：39 道全部重写为逐项解析，并修好第121题解析自相矛盾的硬伤 | — |
| ~~教材考点知识点残缺~~ | ✅ **已完成**：852→599 条，删残句 / 修错字 / 去断词空格（详见第三节 J）。**被 `4db226a1` 截断的原文不可恢复，只能删，不是遗漏** | — |
| 第 88 题（F 分布）双正确答案 | A 与 D 均成立，官方取 A。若日后拿到官方勘误再调整 | 低 |
| 教材「思考与练习」精修 | 本轮只做了断词空格 / ÷ 号 / 粘连题三项清理，仍有 OCR 参差（如个别「A。B，C」标点混乱）。要精修需对照原书 | 低 |

**已完成的旧待办**：✅ 交互形态改逐题练习　✅ 做题记录　✅ 单题重做　✅ 删除限时模考　✅ 换设备同步补上做题记录　✅ 修复手机端布局　✅ 修正 12 道题解析与答案矛盾　✅ 补全 39 道多选题逐项解析　✅ 教材考点残缺清理

---

## 六、交付文件清单与归档

### ⚠️ 首先注意 `/workspace/index.html` 是谁

历史上 `/workspace` 根目录出现过**两个同名 `index.html`**（Excel 竞赛版与 cet4 合并版互相覆盖）。**当前状态**：

- `/workspace/index.html`（**397068 字节**，9-09）= **bigdata-excel-contest 当前线上版**，即线上 `0d030666`（逐题练习 + 教材考点 16 章 599 条 + 12 道单选题解析纠错 + 39 道多选题逐项解析 + 教材 OCR 噪声清理）。
- **cet4 合并版本地已无副本**，只存在于 GitHub 仓库 `cet4-platform@2295780`。需要改它请从仓库拉，别用本地文件误覆盖。
- `/workspace/英语四六级学习平台.html`、`考公学习平台.html` 是更早的独立旧文件，非当前线上版。

### 归档仓库 `handoff-archive`（防沙箱销毁，优先从这里取）

地址：https://github.com/hzj-lesson/handoff-archive
整包下载：https://github.com/hzj-lesson/handoff-archive/archive/refs/heads/main.zip

| 归档内容 | 说明 |
|---|---|
| `bigdata-excel-contest/index.html` | **当前线上版**（209708 字节，逐题练习 + 教材考点 16 章，= `38c4be25`） |
| `交接报告.md` | 本文档 |
| `竞赛冲刺建议.md` | 冲刺一等奖的备考建议 |
| `cet4-merged-platform/index.html` | 四级+四六级合并版 |
| `excel-print/print.html` / `print.pdf` | Excel 竞赛必背知识点打印手册 |
| `dsh-plugin-textbook/` | Harness 插件 + README |
| `demo_data-junior.json` | 插件跑通产出样例 |
| `screenshots/` | 各平台截图 |

### 本地 `/workspace` 主要文件

| 文件 | 用途 |
|---|---|
| `index.html` | **bigdata-excel-contest 线上版（当前）** |
| `竞赛冲刺建议.md` | 冲刺一等奖建议（含时间分配、题型优先级） |
| `交接报告.md` | 本文档 |
| `print.html` / `print.pdf` | Excel 打印手册 |
| `dsh-plugin-textbook/` | Harness 插件 |
| `demo_data-junior.json` | 插件产出样例 |
| `merged_*.png` / `cet4_*.png` / `ec_*.png` | 各平台截图 |
| `教师备课助手_功能打包.zip` | 早期 6 项功能打包（9-01） |
| `人教版九年级英语_部署记录.md` | 早期 14 单元部署记录（**只是某次部署的记录，不是教程**） |
| **`部署操作指南.md`** | ⭐ **「怎么部署到 GitHub」的完整教程**（token 配置 / 两种方式 / 各仓库命令 / 校验 / 排障 / 换沙箱恢复） |
| **`部署工具/deploy.py`** | ⭐ **可直接执行的通用部署脚本**（多文件、自动重试、自动校验）。此前脚本都放 `/tmp`，沙箱销毁即丢，现已固化到工作区并归档 |
| **`部署工具/gh.py`** | ⭐ **GitHub 连接器**：`status` 总览 / `list` / `ls` / `pull` / `clone` / `push` / `log` / `pages`。看仓库、拉文件、查 Pages 构建状态都用它（**不用装 git**，tarball 直下） |

---

## 七、一句话总结给新接手人

> 你有 6 个公开学习平台（全在线）。**Excel 竞赛平台是本轮重点**：已从「计时考试」改造成**逐题刷题形态**（做一道立刻出答案+解析、错自动进错题本、全程保留做题记录与正确率、支持单题重做），并修掉 10 个 bug——其中最致命的是**80 道判断题（28%）此前永远判错**，旧成绩不可信。接手先配 token + 升 Node 到 22.19，改动优先改 `/*DATA*/` 内联 JSON，部署后用 `git/blobs` 的 `size` 校验完整性；文件从 `handoff-archive` 取，别依赖沙箱。
