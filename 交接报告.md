# 账号与平台交接报告

> 生成时间：2026-09-07（GMT+8）
> 适用对象：接手 `hzj-lesson` GitHub 账号的新同事/新账号
> 目的：用一份文档说清「账号里有什么、最近改了什么、怎么部署、还有哪些坑和待办」，让交接不再靠口述。

---

## 一、账号与访问信息

| 项目 | 内容 |
|---|---|
| GitHub 账号 | **hzj-lesson**（全部仓库为 public，且全部开启 GitHub Pages） |
| 仓库总数 | **6 个** |
| 访问令牌 | 存于**当前沙箱** `/tmp/gh_token`（权限 600），**不随账号转移**。新环境需用新账号自己的 token 重新生成：`gh auth token` 或 Personal Access Token，写回同名文件即可 |
| 运行环境 | Node.js 22.19.0（经 nvm 安装，**必须 ≥22.19**，原因见第四节）；puppeteer-core + /usr/bin/chromium 用于无头冒烟测试；tesseract + chi_sim 中文 OCR 已装 |
| 域名 | 根域名站 `hzj-lesson.github.io` 即「教师备课助手」 |

⚠️ **交接重点**：沙箱（含 token、nvm、已装插件）是本机环境，不会跟着账号走。新接手人要在自己的机器/沙箱里重新：① 配 GitHub token；② `nvm install 22.19.0`；③ 重装 dsh 自定义插件（见第三节 C）。

---

## 二、6 个平台清单（全部在线可用）

| # | 仓库名 | 在线地址 (Pages) | 仓库地址 | 最近部署 commit | 说明 |
|---|---|---|---|---|---|
| 1 | bigdata-learning | https://hzj-lesson.github.io/bigdata-learning/ | github.com/hzj-lesson/bigdata-learning | — | 大数据管理与应用·个人学习平台 |
| 2 | kaogong-learning-platform | https://hzj-lesson.github.io/kaogong-learning-platform/ | github.com/hzj-lesson/kaogong-learning-platform | — | 考公学习平台（2028 国考版） |
| 3 | hzj-lesson.github.io | https://hzj-lesson.github.io/ | github.com/hzj-lesson/hzj-lesson.github.io | `046b3658` | 教师备课助手（根域名站，含九年级英语 14 单元、语文 300 章等） |
| 4 | bigdata-excel-contest | https://hzj-lesson.github.io/bigdata-excel-contest/ | github.com/hzj-lesson/bigdata-excel-contest | `7d11c369` | 全国大学生大数据分析技能大赛 Excel 赛项平台（本会话重构） |
| 5 | cet4-platform | https://hzj-lesson.github.io/cet4-platform/ | github.com/hzj-lesson/cet4-platform | `2295780c` | 英语四级 CET-4 备考平台（本会话与四六级平台合并） |
| 6 | english-cet-learning | https://hzj-lesson.github.io/english-cet-learning/ | github.com/hzj-lesson/english-cet-learning | — | **已改为自动跳转到 cet4-platform**（不再独立维护） |

> 注：平台 1、2 本会话未改动，仅作清单记录。平台 3 的 14 单元部署为 9-01 完成（commit `94c376a`），之后又补了语文课时（`046b3658`），属于更早背景，本表只列最新态。

---

## 三、本会话做的主要变更（按平台）

### A. bigdata-excel-contest —— Excel 竞赛平台重构（commit `7d11c369`）

**问题**：原版是「知识点 / 刷题 / 题库实战」三个互相跳转的 Tab，用户点起来跳来跳去。

**改动**：
- 去掉多 Tab，改为**一条向下滚动的学习路线**，从第一个知识点排到最后一个，知识点与配套题同屏。
- 12 个学习单元按 42 天备考计划串联，每单元带 `📅 第 X 天 · 9月Y日` 徽章；函数/透视表/文本日期作补充专题单元。
- **题目全局去重**：原 643 题 → 删除 11 道重复 → **632 道唯一题**（学习路线内嵌 252 题 + 专项/整卷 380 题）。
- 顶部进度条 + 本地错题本。
- **另交付可打印版**：`print.html`（带打印按钮）+ `print.pdf`（A4，9 页，66 条必背要点）。

**文件**：`/workspace/index.html`（即线上版）、`/workspace/print.html`、`/workspace/print.pdf`

**遗留**：仓库内旧 `files/` 资源（打印手册 PDF、Excel 素材）已不再被新页面引用，仍在仓库未删；单元 12「数据建模」无专属题（原题库没有建模模块），练习并到了数据预处理单元。

### B. cet4-platform + english-cet-learning —— 合并去重（commit `2295780c`）

**决策**：主站用 `cet4-platform`（内容最实、URL 短），以四级为主，立即部署；`english-cet-learning` 改为跳转。

**改动**：
- 合并后 **9 个模块**：总览 / 逐日计划 / 知识点 / 在线刷题 / 备考方法 / 资源中心 / **单词本** / **笔记日志** / **番茄钟**（实际把热力图并入逐日计划、闪卡并入单词本）。
- 从四六级平台吸收并去重：**单词本 669 词**（音标+释义+例句，可搜索、按掌握度标记）、**闪卡复习**、**🍅 番茄钟**、**📝 笔记日志**（支持导出/导入 JSON 备份）、**🔥 打卡热力图**。
- 保留四级平台全套：四阶段 104 天逐日计划、必背知识点、60 题在线刷题、倒计时。
- **资源中心去重合并**：按 URL 去重（官网重复只留一条），新增 12 条实用资源，共 14 条。
- `english-cet-learning/index.html` 改为 `<meta http-equiv="refresh" content="0; url=https://hzj-lesson.github.io/cet4-platform/">`，旧用户自动跳转。

**文件**：`/workspace/index.html`（合并后线上版）、多张截图 `merged_*.png`。

### C. DeepSeek Harness —— 自定义插件「教材→讲义」流水线

**做了什么**：在官方开源 Agent 底座 [deepseek-ai/deepseek-harness](https://github.com/deepseek-ai/deepseek-harness) 上写了一个自定义 cordis 插件 `dsh-plugin-textbook`，跑通了「教材大纲 → 结构化讲义 → 注入平台数据层」的自动化管线。

**验证结果**（沙箱真实跑通，日志为证）：
```
[dsh-plugin-textbook] CONSTRUCTED (service name=textbook)
[dsh-plugin-textbook] DEMO 完成 -> {path:/workspace/demo_data-junior.json, total:1, insertedAt:0}
```
插件被 Harness 在运行时加载、解析了九年级英语 Unit1 大纲、生成讲义 JSON 并注入数据文件。产出 `demo_data-junior.json` 字段完全对齐备课平台 `data-junior` 模式（备课方案 / 核心知识点表格 / 重点难点 / 典型例题 / 易·中·较难·难 4 档作业 / 课后反馈）。

**交付物**：
- `/workspace/dsh-plugin-textbook/`（package.json + lib/index.js + README.md）
- `/workspace/demo_data-junior.json`（跑通后的真实产出）

**插件安装约定（接手必读）**：
1. Node 必须 **≥ 22.19**（沙箱原 22.13 缺 `node:zlib.createZstdDecompress`，Harness 启动即失败）。用 nvm：`nvm install 22.19.0 && nvm use 22.19.0`。
2. 插件须放在 profile 的 node_modules 链：`~/.dsh/profiles/web/node_modules/@deepseek-ai/dsh-plugin-textbook`（只丢进 npx 缓存会被 loader 找不到）。
3. 在 `~/.dsh/profiles/web/cordis.patch.yml` 的 `dispose` 列表里加 `- name: '@deepseek-ai/dsh-plugin-textbook'`（注意 patch 非 insert 项必须有 `id`）。
4. 入口是 `export default class XService extends Service`，cordis 自动 `ctx.plugin` 激活；**不能写 TS 的 `declare module`**（纯 JS 运行时不认）。
5. 启动：`DSH_DEMO=1 npx @deepseek-ai/dsh web`（浏览器开 3080）。
6. `lib/index.js` 里 `generateWithLLM` 为占位，**尚未接真实 LLM Key**——接上 DeepSeek API 后即可变成真·AI 自动备课。

---

## 四、关键技术约定（接手必读）

1. **单文件前端 + React 打包模式**：各学习平台多为单 `index.html`，内部引用 Vite 打包 chunk（如 `data-junior-*.js`、`index-*.js`）。改内容常是改注入的数据数组再重新打包/部署。
2. **数据层结构**（各平台命名不同，需逐个看）：
   - 教师备课助手：`data-junior`（讲义）、`data-meta`（目录）、`wb-lib`。
   - Excel 竞赛：`QZBANK`（题）、`KB1/KB2`（知识点）、`TIPS`。
   - 四级/四六级：`ALLQ`（题）、`VOCAB`（词）、`STAGES`（阶段）、`RES`（资源）、`state`（打卡）。
3. **部署方式**：
   - 单文件更新 → **GitHub Contents API**（`PUT /repos/{owner}/{repo}/contents/{path}`，需 base64 + 文件 sha）。
   - 多文件批量 → **Git Data API**（blob→tree→commit→ref），支持 429/5xx 重试与续传缓存（参考历史 `deploy.py`）。
   - 沙箱网络对 GitHub 需 `/etc/hosts` 绑定 + `Connection: close` 头绕过 SNI/keep-alive 故障。
4. **命名与主权**：台湾/香港/澳门统一称「中国台湾/中国香港/中国澳门」，勿作独立国家表述。

---

## 五、待办 / 未完事项（建议新接手人推进）

| 项 | 说明 | 优先级 |
|---|---|---|
| 北师大数学九上 6 章 | 目前仅目录（6 章标题），未补完整教案（备课方案/例题/作业）。仓库历史有 `a228b840` | 中 |
| Excel 竞赛「数据建模」单元 | 无专属题，临时并到数据预处理单元 | 低 |
| 百度网盘 PDF 提取 | 用户想提取一份图片型 PDF 知识点做打印版，**因文件 >20MB 且为扫描图，未传文件，未完成**。路径：用户本地用 Umi-OCR 识别成 txt 发来，或压到 <20MB 上传后我 tesseract 识别 | 取决于用户 |
| bigdata-excel-contest 旧资源 | 仓库 `files/` 已不被新页面引用，建议清理瘦身 | 低 |
| DeepSeek Harness 接 LLM | `generateWithLLM` 占位未接真实 Key，接上即变 AI 备课 | 中（需 Key） |
| 打印版风格统一 | Excel 竞赛已出 print 版；其他平台（如四级必背）可按需同样生成 | 按需 |

---

## 六、本会话交付文件清单（均在 `/workspace`）

| 文件/目录 | 用途 |
|---|---|
| `index.html`（168KB） | cet4+四六级合并后的统一平台线上版 |
| `index.html`（258KB） | bigdata-excel-contest 重构后的线上版（即 `/workspace` 根，注意两个 index.html 同名，前者覆盖） |
| `print.html` / `print.pdf` | Excel 竞赛必背知识点可打印手册（HTML + A4 PDF 9 页） |
| `dsh-plugin-textbook/` | DeepSeek Harness 自定义插件（教材→讲义流水线）+ README |
| `demo_data-junior.json` | Harness 插件跑通后自动产出的讲义样例 |
| `merged_*.png` / `cet4_*.png` / `ec_*.png` | 各平台渲染预览/截图 |
| `教师备课助手_功能打包.zip` | 早期 6 项功能打包（9-01） |
| `人教版九年级英语_部署记录.md` | 早期 14 单元部署记录 |

> 注意：`/workspace` 根目录同时存在两个同名 `index.html`（先写的是 Excel 竞赛版 258KB，后被合并版 168KB 覆盖）。交接时以 **168KB 的 cet4 合并版** 为准；Excel 竞赛的线上版以仓库 `bigdata-excel-contest@7d11c369` 为准，勿用本地旧文件误覆盖。

---

## 七、一句话总结给新接手人

> 你有 6 个公开学习平台（全在线）。本会话把**Excel 竞赛**重做成单路线去重版、**四级与四六级合并**成统一平台（旧站自动跳转）、并**验证跑通了 DeepSeek Harness 的教材→讲义插件**作为未来内容自动化底座。接手先配 token + 升 Node 到 22.19，再按需推进「北师大教案补全 / Harness 接 LLM / 各平台打印版」等遗留项。
