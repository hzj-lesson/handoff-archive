# 账号与平台交接报告

> 生成时间：2026-09-08 16:00（GMT+8）　|　上一版：2026-09-07
> 适用对象：接手 `hzj-lesson` GitHub 账号的新同事/新账号
> 目的：用一份文档说清「账号里有什么、最近改了什么、怎么部署、还有哪些坑和待办」，让交接不再靠口述。
> **本版更新重点**：Excel 竞赛平台（`bigdata-excel-contest`）交互形态重构为「逐题练习」+ 修复 10 个 bug + 删除限时模考 + 新增单题重做。见第三节 A、B。

---

## 一、账号与访问信息

| 项目 | 内容 |
|---|---|
| GitHub 账号 | **hzj-lesson**（全部仓库为 public，且全部开启 GitHub Pages） |
| 仓库总数 | **6 个**（另有 1 个归档仓库 `handoff-archive`） |
| 访问令牌 | 存于**当前沙箱** `/tmp/gh_token`，**不随账号转移**。新环境需用自己的 token 重新生成：`gh auth token` 或 Personal Access Token，写回同名文件即可 |
| 运行环境 | Node.js 22.19.0（经 nvm 安装，**必须 ≥22.19**，原因见第三节 E）；puppeteer-core + `/usr/bin/chromium` 用于无头冒烟测试；tesseract + chi_sim 中文 OCR 已装 |
| 域名 | 根域名站 `hzj-lesson.github.io` 即「教师备课助手」 |

⚠️ **交接重点**：沙箱（含 token、nvm、已装插件）是本机环境，不会跟着账号走。新接手人要在自己的机器/沙箱里重新：① 配 GitHub token；② `nvm install 22.19.0`；③ 重装 dsh 自定义插件（见第三节 E）。

⚠️ **沙箱销毁风险**：`/workspace` 里的文件在沙箱关闭后会丢失。**关键交付物已归档到 `handoff-archive` 仓库**（见第六节），接手人优先从该仓库取，不要依赖本地沙箱。

---

## 二、6 个平台清单（全部在线可用）

| # | 仓库名 | 在线地址 (Pages) | 最近部署 commit | 说明 |
|---|---|---|---|---|
| 1 | bigdata-learning | https://hzj-lesson.github.io/bigdata-learning/ | — | 大数据管理与应用·个人学习平台 |
| 2 | kaogong-learning-platform | https://hzj-lesson.github.io/kaogong-learning-platform/ | — | 考公学习平台（2028 国考版） |
| 3 | hzj-lesson.github.io | https://hzj-lesson.github.io/ | `046b3658` | 教师备课助手（根域名站，九年级英语 14 单元、语文 300 章等） |
| 4 | **bigdata-excel-contest** | https://hzj-lesson.github.io/bigdata-excel-contest/ | **`245ce2b`** | 全国大学生大数据分析技能大赛 Excel 赛项平台（**本轮重点重构**） |
| 5 | cet4-platform | https://hzj-lesson.github.io/cet4-platform/ | `2295780` | 英语四级 CET-4 备考平台（与四六级平台合并后） |
| 6 | english-cet-learning | https://hzj-lesson.github.io/english-cet-learning/ | — | **已改为自动跳转到 cet4-platform**（不再独立维护） |

> 平台 1、2 未改动，仅作清单记录。仓库地址均为 `github.com/hzj-lesson/<仓库名>`。

### `bigdata-excel-contest` 版本演进（接手人看这里就知道改到哪了）

| commit | 日期 | 内容 |
|---|---|---|
| `7d11c369` | 9-07 | 重构为单线学习路线，632 题去重 |
| `2c4ced9d` | 9-08 | 精简：剥离零散题与专项，**只保留两套官方模拟题**（285 题） |
| `03a47e68` | 9-08 | 修复判断题判分等 bug，加计时模考 |
| `49d289c` | 9-08 | **交互形态改为逐题练习** + 做题记录面板 |
| `4a8509f` | 9-08 | 修复 7 处一致性 bug |
| `245ce2b` | 9-08 | **彻底删除限时模考** + 新增单题重做（当前线上版） |

---

## 三、变更记录（按时间倒序）

### A.（9-08 本轮重点）bigdata-excel-contest 交互形态重构

**用户诉求**：「我要变成那种做题形式而不是考试形式，做一道出一道答案，然后做错了进入错题本，要保留做题记录。」

**改动**：

1. **逐题练习模式**（替代原计时模考成为主入口）
   - 每套卷主按钮「📝 开始练习」→ 全屏逐题：点选项**立即出答案**（✅/❌ + 高亮正确项 + 解析），多选题点「提交答案」后判分。
   - 右侧实时看板：本卷已练数、当前正确率、题号彩色导航（绿=对 / 红=错 / 灰=未练），可跳题。
   - **接着上次进度**：重新进入自动跳到第一道未练的题，并提示「已跳到第 N 题接着练」。

2. **做题记录面板**（新增「📊 做题记录」区块）
   - 总练习题数、总正确率（≥80% 绿、否则橙）；各卷进度 + 最近练习时间；**最近 30 条练习流水**（卷别+第几题+✅/❌+时间）。
   - 全程持久化，刷新/关页不丢。

3. **单题重做**：练习模式有「🔄 重做本题」，卷子展开区每题有「🔄 重做」。重做后按新结果重新计入记录（重做答对，记录从"错"改回"对"）。

4. **彻底删除限时模考**：删掉「⏱ 限时模考」按钮、123 行考试 JS（startExam/交卷评分/倒计时/答题卡）、考试专属 CSS（成绩报告/计时器/脉冲动画）。文件从 204005 → **196415 字节**。
   > 注意：`.exam` 那套**布局样式保留**（逐题练习的全屏界面复用了它，删了界面就散了）。

5. **统一答题入口**：新增 `commitAnswer()`，让「逐题练习 / 展开直接点 / 错题重练」三条路径行为完全一致（此前各写各的，导致行为不一致，见 Bug #9）。

### B.（9-08）Bug 修复清单 —— 共 10 个

**数据层审计结论：题库 285 题零异常**（无重复 id、无重复题干、答案无越界、无缺解析、两卷无 id 冲突）。问题全在代码逻辑层。

| # | 严重度 | 问题 | 影响 |
|---|---|---|---|
| 1 | 🔴 致命 | **判断题判分逻辑写反** | 80 道判断题（占 285 题的 28%）答案存字母 `A/B`，但旧代码拿选项文字「正确/错误」和字母比，永远不相等 → **判断题永远判错、正确答案永远不亮、错题本全是假错题**。改为统一按字母判分 |
| 2 | 🟠 | 模考计时显示错误 | `("0"+120).slice(-2)` 得 `"20"` → 显示成 `20:00`（已随模考删除） |
| 3 | 🟡 | 各卷「最近练习」永远显示 `—` | `recAnswer` 漏写 `r.last` 时间戳，而面板读的正是它 |
| 4 | 🔴 | **换设备同步会丢做题记录** | 同步码只打包「学习进度+错题本」，没打包做题记录和已练进度 → 换设备后记录归零（与"保留做题记录"直接冲突）。已升级同步码到 v3 |
| 5 | 🟠 | 重新进入练习不接着上次 | 每次从第 1 题开始，而答过的题锁定，得一路点几十次「下一题」 |
| 6 | 🟡 | 重置本卷后顶栏「已练」不回退 | 只清了记录没清 `answered` |
| 7 | 🟠 | 错题重练答对了，记录仍记为错 | 重练只移出错题本，不回写记录 → 正确率不反映真实水平 |
| 8 | 🟡 | 限时模考交卷不计入记录 | 顶栏「已练」涨了，记录纹丝不动（已随模考删除） |
| 9 | 🔴 | **展开题直接点，答错不进错题本** | 只有走「开始练习」才进错题本；在卷子下方展开题目直接点，只算"已练"，**答错既不进错题本也不进记录** |
| 10 | 🟡 | 当场答完的题不显示「重做」按钮 | 按钮是渲染时按记录生成的，当场作答只加了 `done` 类没重渲染 → 只有刷新后才出现。已改为答完就地重渲染 |

> Bug #1 是本轮最值钱的发现：**如果你之前用旧版练过题，当时的分数不能信**（近三成题目永远判错）。

### C.（9-07）bigdata-excel-contest 单线学习路线重构（commit `7d11c369`）

- 去掉多 Tab 跳转，改为**一条向下滚动的学习路线**，知识点与配套题同屏。
- 12 个单元按 42 天备考计划串联，每单元带 `📅 第 X 天 · 9月Y日` 徽章。
- 题目全局去重：原 643 题 → 删 11 道重复 → 632 道。
- 另交付可打印版 `print.html` + `print.pdf`（A4，9 页，66 条必背要点）。
- **遗留**：仓库内旧 `files/` 资源已不被新页面引用，建议清理；单元 12「数据建模」无专属题，练习并到数据预处理单元。

### D.（9-07）cet4-platform + english-cet-learning 合并（commit `2295780`）

- 主站用 `cet4-platform`，`english-cet-learning` 改为 `<meta http-equiv="refresh">` 自动跳转。
- 合并后 9 模块：总览 / 逐日计划 / 知识点 / 在线刷题 / 备考方法 / 资源中心 / 单词本（669 词）/ 笔记日志 / 番茄钟，另含闪卡复习、打卡热力图。
- 资源中心按 URL 去重，新增 12 条实用资源。

### E. DeepSeek Harness 自定义插件「教材→讲义」

在官方开源底座 [deepseek-ai/deepseek-harness](https://github.com/deepseek-ai/deepseek-harness) 上写了 cordis 插件 `dsh-plugin-textbook`，跑通「教材大纲 → 结构化讲义 → 注入平台数据层」。

验证日志（沙箱真实跑通）：
```
[dsh-plugin-textbook] CONSTRUCTED (service name=textbook)
[dsh-plugin-textbook] DEMO 完成 -> {path:/workspace/demo_data-junior.json, total:1, insertedAt:0}
```

**安装约定（接手必读）**：
1. Node 必须 **≥ 22.19**（沙箱原 22.13 缺 `node:zlib.createZstdDecompress`，启动即失败）：`nvm install 22.19.0 && nvm use 22.19.0`。
2. 插件须放在 `~/.dsh/profiles/web/node_modules/@deepseek-ai/dsh-plugin-textbook`（丢进 npx 缓存会被 loader 找不到）。
3. 在 `~/.dsh/profiles/web/cordis.patch.yml` 的 `dispose` 列表加 `- name: '@deepseek-ai/dsh-plugin-textbook'`。
4. 入口是 `export default class XService extends Service`；**不能写 TS 的 `declare module`**（纯 JS 运行时不认）。
5. 启动：`DSH_DEMO=1 npx @deepseek-ai/dsh web`（浏览器开 3080）。
6. `lib/index.js` 里 `generateWithLLM` 为占位，**尚未接真实 LLM Key**。

---

## 四、关键技术约定（接手必读）

1. **单文件前端 + 数据注入**：`bigdata-excel-contest` 是单 `index.html`，数据在 `/*DATA*/{...}/*DATA*/` 标记内以 JSON 内联。改题 = 改这段 JSON。
   - **当前顶层结构**：`units`（12 个学习单元，现已无内嵌题）+ `practices`（两套卷：`pr15` 省赛模拟卷一 146 题 / `pr16` 省赛模拟卷二 139 题，共 **285 题**）+ `tips`。
   - 题型分布：判断 80 / 单选 166 / 多选 39。
   - ⚠️ 旧文档写的 `QZBANK` / `KB1` / `KB2` 是**精简前的旧结构，已不存在**，勿按此找。
   - 单题字段：`q`(题干) `o`(选项数组) `a`(答案字母，多选如 `"ABD"`) `type`(`single`/`multi`/`judge`) `ex`(解析) `id`。
   - **答案一律以字母存储**（A/B/C/D）。判断题选项「正确/错误」对应 A/B —— 这正是 Bug #1 的根源，改判分逻辑时务必注意。

2. **localStorage 键位**（`bigdata-excel-contest` 全部状态，清缓存即丢）：
   | 键 | 用途 |
   |---|---|
   | `excel_route_v2` | 单元完成状态 |
   | `excel_route_wrong_v2` | 错题本 |
   | `excel_route_doneq_v2` | 已练题集合（顶栏「已练 X/285」） |
   | `excel_practice_records_v2` | **做题记录**（各卷 answers + 最近 60 条流水 + last 时间戳） |

3. **其他平台数据层**：教师备课助手 `data-junior` / `data-meta` / `wb-lib`；四级/四六级 `ALLQ` / `VOCAB` / `STAGES` / `RES` / `state`。

4. **部署方式**：
   - 单文件更新 → **GitHub Contents API**（`PUT /repos/{owner}/{repo}/contents/{path}`，需 base64 + 文件 sha）。
   - 多文件批量 / 精确控制 → **Git Data API**（blob→tree→commit→ref）。
   - 沙箱网络对 GitHub 需 `/etc/hosts` 绑定 + `Connection: close` 头绕过 SNI/keep-alive 故障。
   - ⚠️ **校验部署是否完整**：用 `git/blobs/<sha>` 的 `size` 字段对比本地字节数。**不要**用 base64 解码后的 `.length` 判断——那是 UTF-8 字符数，中文占多字节，会误报"文件被截断"（本轮踩过两次这个坑）。

5. **测试方法**：puppeteer-core + `/usr/bin/chromium` 无头跑真实交互。**注意页面里有 `confirm()` 的按钮（如「重置本卷」）必须注册 `page.on('dialog')` 自动接受，否则测试会挂死**。

6. **命名与主权**：中国台湾/中国香港/中国澳门，勿作独立国家表述。

---

## 五、待办 / 未完事项

| 项 | 说明 | 优先级 |
|---|---|---|
| 百度网盘图片 PDF 提取 | 用户想提取一份图片型 PDF 知识点做打印版，**因文件 >20MB 且为扫描图，用户未传文件，未完成**。路径：本地用 Umi-OCR 识别成 txt 发来，或压到 <20MB 上传后 tesseract 识别 | 取决于用户 |
| DeepSeek Harness 接 LLM | `generateWithLLM` 占位未接真实 Key，接上即变 AI 自动备课 | 中（需 Key） |
| 北师大数学九上 6 章 | 目前仅目录，未补完整教案。仓库历史有 `a228b840` | 中 |
| Excel 竞赛「数据建模」单元 | 无专属题，临时并到数据预处理单元 | 低 |
| bigdata-excel-contest 旧 `files/` | 已不被新页面引用，建议清理瘦身 | 低 |
| 各平台打印版 | Excel 竞赛已出 print 版；其他平台可按需生成 | 按需 |

**已完成的旧待办**：✅ 交互形态改逐题练习　✅ 做题记录　✅ 单题重做　✅ 删除限时模考　✅ 换设备同步补上做题记录

---

## 六、交付文件清单与归档

### ⚠️ 首先注意 `/workspace/index.html` 是谁

历史上 `/workspace` 根目录出现过**两个同名 `index.html`**（Excel 竞赛版与 cet4 合并版互相覆盖）。**当前状态**：

- `/workspace/index.html`（**196415 字节**，9-08 15:45）= **bigdata-excel-contest 逐题练习版**，即线上 `245ce2b`。
- **cet4 合并版本地已无副本**，只存在于 GitHub 仓库 `cet4-platform@2295780`。需要改它请从仓库拉，别用本地文件误覆盖。
- `/workspace/英语四六级学习平台.html`、`考公学习平台.html` 是更早的独立旧文件，非当前线上版。

### 归档仓库 `handoff-archive`（防沙箱销毁，优先从这里取）

地址：https://github.com/hzj-lesson/handoff-archive
整包下载：https://github.com/hzj-lesson/handoff-archive/archive/refs/heads/main.zip

| 归档内容 | 说明 |
|---|---|
| `bigdata-excel-contest/index.html` | **本轮最终版**（196415 字节，逐题练习） |
| `交接报告.md` | 本文档 |
| `竞赛冲刺建议.md` | 冲刺一等奖的备考建议 |
| `cet4-merged-platform/index.html` | 四级+四六级合并版 |
| `excel-print/print.html` / `print.pdf` | Excel 竞赛必背知识点打印手册 |
| `dsh-plugin-textbook/` | Harness 插件 + README |
| `demo_data-junior.json` | 插件跑通产出样例 |
| `screenshots/` | 各平台截图 |

### 本地 `/workspace` 主要文件

| 文件 | 用途 |
|---|---|
| `index.html` | **bigdata-excel-contest 线上版（当前）** |
| `竞赛冲刺建议.md` | 冲刺一等奖建议（含时间分配、题型优先级） |
| `交接报告.md` | 本文档 |
| `print.html` / `print.pdf` | Excel 打印手册 |
| `dsh-plugin-textbook/` | Harness 插件 |
| `demo_data-junior.json` | 插件产出样例 |
| `merged_*.png` / `cet4_*.png` / `ec_*.png` | 各平台截图 |
| `教师备课助手_功能打包.zip` | 早期 6 项功能打包（9-01） |
| `人教版九年级英语_部署记录.md` | 早期 14 单元部署记录 |

---

## 七、一句话总结给新接手人

> 你有 6 个公开学习平台（全在线）。**Excel 竞赛平台是本轮重点**：已从「计时考试」改造成**逐题刷题形态**（做一道立刻出答案+解析、错自动进错题本、全程保留做题记录与正确率、支持单题重做），并修掉 10 个 bug——其中最致命的是**80 道判断题（28%）此前永远判错**，旧成绩不可信。接手先配 token + 升 Node 到 22.19，改动优先改 `/*DATA*/` 内联 JSON，部署后用 `git/blobs` 的 `size` 校验完整性；文件从 `handoff-archive` 取，别依赖沙箱。
